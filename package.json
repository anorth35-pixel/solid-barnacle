{
  "name": "cribbgolf",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "npm run build -w packages/shared && concurrently \"npm run dev -w packages/server\" \"npm run dev -w packages/client\"",
    "build": "npm run build -w packages/shared && npm run build -w packages/server && npm run build -w packages/client",
    "build:prod": "npm run build -w packages/shared && npm run build -w packages/server && npm run build -w packages/client",
    "start": "NODE_ENV=production node packages/server/dist/index.js",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "socket.io-client": "^4.6.1",
    "typescript": "^5.3.3"
  }
}
